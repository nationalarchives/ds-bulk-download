services:
  app:
    build:
      context: .
      args:
        IMAGE: ghcr.io/nationalarchives/tna-python-dev
        IMAGE_TAG: preview
    environment:
      - ENVIRONMENT_NAME=localhost
      - CONFIG=config.Develop
      - BUILD_VERSION=local
      - DEBUG=true
      - SECRET_KEY=abc123
      - NPM_DEVELOP_COMMAND=dev
      - COOKIE_DOMAIN=localhost
      - S3_ENDPOINT=http://minio:9000
      - S3_EXPORT_BUCKET=export
      - S3_MERLIN_SOURCE=merlin
      - AWS_ACCESS_KEY_ID=minioadmin
      - AWS_SECRET_ACCESS_KEY=minioadmin
      - AWS_DEFAULT_REGION=us-east-1
    ports:
      - 65491:8080
    volumes:
      - ./:/app
    depends_on:
      - minio
    healthcheck:
      start_period: 10s
      start_interval: 2s

  minio:
    image: quay.io/minio/minio
    ports:
      - 65490:9001
    environment:
      MINIO_ROOT_USER: admin
      MINIO_ROOT_PASSWORD: password123
    restart: always
    volumes:
      - ./s3/data:/data
    command: server /data --console-address ":9001"

  minio-init:
    image: minio/mc
    environment:
      MINIO_ROOT_USER: admin
      MINIO_ROOT_PASSWORD: password123
      ACCESS_KEY: minioadmin
      SECRET_KEY: minioadmin
    depends_on:
      - minio
    entrypoint: sh /init.sh
    volumes:
      - ./s3/init.sh:/init.sh

  docs:
    image: squidfunk/mkdocs-material
    volumes:
      - .:/docs
    ports:
      - 65492:8000

  # create-lambda:
  #   image: python:3.14@sha256:812b177f7c41214cbfa20a9cf13df476dab8f3012f811832d325feaa25bf7878
  #   volumes:
  #     - ./:/app
  #   working_dir: /app
  #   entrypoint: sh -c "apt-get -y update && apt-get -y install zip && sh ./tasks/create_lambda.sh"
